
pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
    }
}

rootProject.name = 'Kabu'

include(
        "annotation",
        "backend",
        "runtime",
        "frontend:ksp:processor",
        "frontend:ksp:testing",
)

